/**
 * Shared prompt text used when starting a new chat from a CTA (e.g. Home, Artifact Library).
 * Single source of truth so copy stays in sync and tests can import for assertions.
 */
export const CREATE_DASHBOARD_PROMPT = 'Can you help me create a new dashboard?';
